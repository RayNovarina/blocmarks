<header>
  <div class="row">
    <div class="col-md-8 css-page_title">
      <div class="media">
        <div class="media-body">
          <h2 class="media-heading" title='<%= props[:bookmark].new_record? ? "" : "Created by: #{props[:bookmark].user.name}" %>'>
            <%= props[:bookmark].new_record? ? 'New' : 'Edit' %> Bookmark
          </h2>
        </div>
      </div>
    </div>
  </div>
</header>

<main>
  <div class="row">
    <div class="col-md-4">
      <p>Guidelines for bookmarks:</p>
      <ul>
        <li>Make sure the topic is a good match.</li>
        <li>Description should indicate what this bookmark leads to.
        </li>
        <li>Bookmarks can be edited or deleted by their creator at any time.</li>
      </ul>
    </div>
    <div class="col-md-4">
      <%= render partial: 'new_edit_parent_topic', locals: { props: props } %>
      <%= render partial: 'shared/errors',
                 locals: { props: props.merge!(obj: props [:bookmark]) } %>
      <%= render partial: 'new_edit_form', locals: { props: props } %>
    </div>
  </div>
</main>
